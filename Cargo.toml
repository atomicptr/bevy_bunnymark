[package]
name = "bevy_bunnymark"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.11", features = ["dynamic_linking"] }
bevy_pixel_camera = "0.5.0"
rand = "0.8.5"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

